## Frontend environment variables
## Copy to .env if you want to override API base.
##
## Default behavior (leave empty):
## - In dev, Vite proxy in `vite.config.js` can handle `/chat`, `/stats`, `/health`
## - In prod, deploy behind the same origin or set VITE_API_BASE

# VITE_API_BASE=http://localhost:8000
